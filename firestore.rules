rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development: permite leitura/escrita para todos (INSEGURA — apenas para desenvolvimento)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
